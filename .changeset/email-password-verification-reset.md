---
"create-betternest-app": minor
---

Add email verification and password reset to the email-password provider. Generated projects now ship a new swappable `@repo/email` package (Resend), forgot-password / reset-password pages, verification-aware sign-in and sign-up, and `RESEND_API_KEY` / `EMAIL_FROM` config — all added only when the email-password provider is selected.

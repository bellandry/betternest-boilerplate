---
'create-betternest-app': minor
---

Add an idempotent admin seed, Better Auth 1.7 Account identity schema alignment, a conflict-safe update command, and `--skip-auth`, `--skip-email`, and `--skip-ui` generation options.
